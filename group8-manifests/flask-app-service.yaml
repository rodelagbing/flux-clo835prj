kind: Service
apiVersion: v1
metadata:
  name: webapp-service
  namespace: final
  labels:
    name: flask-app
spec:
  selector:
    name: flask-app
  type: loadBalancer
  ports:
  - port: 80
    targetPort: 81